Traversal
Traversal means visiting every element of a data structure one by one.
For an array, traversal means starting from the first element and moving to the last element.

We traverse arrays to perform operations such as:

Finding the maximum or minimum element
Calculating the sum
Counting occurrences of a value
Searching for an element
Copying elements to another array
Modifying elements
